export default function HeroSection() {
	return (
		<section className="hero-section background-image h-screen">
			<div className="container flex items-center justify-center h-full">
				<h2 className="hero-title text-center">EVENTS</h2>
			</div>
		</section>
	);
}
